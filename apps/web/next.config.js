/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["three"],
  allowedDevOrigins: ["127.0.0.1", "*.corpoealmahumanizado.com.br", "estudio-agenda-teste.vercel.app"],
};

export default nextConfig;
