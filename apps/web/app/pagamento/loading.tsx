import { PageSkeleton } from "../../components/ui/loading-system";

export default function PagamentoRootLoading() {
  return <PageSkeleton title="Carregando página de pagamento..." sections={1} />;
}
