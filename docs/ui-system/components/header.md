# AppHeader

Variações:
- Home (saudação + status + título + ações)
- List (título + ação)
- Form (botão voltar + título/subtítulo)

Regras:
- Sticky, fundo branco, `shadow-soft`, `rounded-b-3xl`, `safe-top`.
