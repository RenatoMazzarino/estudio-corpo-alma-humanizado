sonar.projectKey=RenatoMazzarino_estudio-corpo-alma-humanizado
sonar.organization=renatomazzarino

sonar.sourceEncoding=UTF-8
sonar.sources=.

# Reduce noise from generated artifacts, lockfiles and docs.
sonar.exclusions=**/node_modules/**,**/.next/**,**/.turbo/**,**/dist/**,**/build/**,**/coverage/**,pnpm-lock.yaml,docs/**

# Coverage can be enabled later when a stable report path exists.
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
